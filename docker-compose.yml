version: '3.8'

services:
  retinax-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: retinax-ai-platform
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
